
***Order of gathering property info***

1.) Main scan from update properties
    var = UpdateProperties.new
    var.run
    ---updates database (add and remove)---

in blocks of ten per hour maybe?
find one with blank attributes
2.) UpdateParcel.get_aerial(property)
    ---fixes address formatting for zillow ---
3.) UpdateZillow.get_valuation
    UpdateZillow.get_prop_details

add errors for no property found
add when auction will be
create pre-header with introduction and quick rundown of the site and some widgets
get icon & site name image

create mouseover options
add menu bar options
add 'search' to menu bar



Navbar:


Index Page:
hide map option
      make markers have popups of property
  index, list

show page
get assessors map
Tax bill snapshot?

get missing .png
do not update picture if available

add street view link / layer

FILTERS:
one giant index page

display options:

  all
  remove 'land'

create 'search'
